import React from "react";

function Functionprops(props) {
    return(
        <div>
            <h1>Hello {props.name} from {props.place}. Welcome to Simplilearn!</h1>
        </div>
    );
}

export default Functionprops;